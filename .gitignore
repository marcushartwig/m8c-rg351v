# On-device build checkout
ports/M8/.src/

# Built artefacts (prebuilt/ is deliberately tracked - it is the archive)
ports/M8/m8c/m8c
ports/M8/m8c/gamecontrollerdb.txt
build/out/
dist/
*.zip

.DS_Store

# Vendored .debs (fetched by scripts/fetch-deps.sh)
vendor/

# Third-party firmware download (Dirtywave EULA, not ours to redistribute)
firmware/
